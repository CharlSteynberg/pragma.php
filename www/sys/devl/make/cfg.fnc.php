<?

// def :: make.cfg - extends: `devl`
// --------------------------------------------------------------------------------------
   set::{'devl.make.cfg'}
   (
   // fnc :: cfg - implements: `devl::make.cfg()`
   // -----------------------------------------------------------------------------------
      function($src,$pth)
      {
      // rsl :: return - updated `$src`
      // --------------------------------------------------------------------------------
         return $src;
      // --------------------------------------------------------------------------------
      }
   // -----------------------------------------------------------------------------------
   );
// --------------------------------------------------------------------------------------

?>
